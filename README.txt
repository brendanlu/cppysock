Some simple functions, scripts and examples of interprocess communication via 
sockets. Include folder contains header-only implementations (basically thin 
wrappers of the underlying socket API's), and is intended to be C/C++ and 
windows/*nix compatible. 
